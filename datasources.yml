# Grafana
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    # PROMETHEUS_HOST should be set to "qa-test_prometheus" in Swarm or "prometheus" in Compose
    url: http://${PROMETHEUS_HOST}:9090
    isDefault: true