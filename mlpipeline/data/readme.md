## Recipes.csv

* the str.len of `RecipeIngredientQuantities` and `RecipeIngredientParts`, we can consider only
  use `RecipeIngredientParts` and discard the quantities of the ingredients.

## Reviews.csv

* regarding the `Rating` we only have *2/NA*, but from the description they said the rating is about the difficulties of
  the recipes and range from 1-5, is this information usable for us?

* `Liking`: 1->True, 0->False