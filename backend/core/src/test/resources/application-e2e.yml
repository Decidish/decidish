spring:
#   datasource:
#     url: jdbc:postgresql://localhost:5433/decidish # Port 5433 (External)
#     username: user
#     password: 1234
#     driver-class-name: org.postgresql.Driver

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: validate # Don't drop schema in E2E, just validate