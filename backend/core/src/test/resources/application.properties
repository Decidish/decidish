# # src/test/resources/application.properties

# Use the fixed port (5433) and the hardcoded credentials from .env

# ! Note: this are from your .env file so CHANGE THEM IF NEEDED !

# POSTGRES_USER="user"
# POSTGRES_PASSWORD="password"
# POSTGRES_DB="decidish"

spring.datasource.url=jdbc:postgresql://localhost:5433/decidish
spring.datasource.username=user
spring.datasource.password=password

# ! Doesn't work
# spring.datasource.url=jdbc:postgresql://localhost:5434/${POSTGRES_DB}
# spring.datasource.username=${POSTGRES_USER}
# spring.datasource.password=${POSTGRES_PASSWORD}

# Ensure a clean database for every test
spring.jpa.hibernate.ddl-auto=create-drop
